#include "output.h"
#include "input.h"
#include "backend.h"
#include "core.h"

int main(int argc, char **argv) {
    std::vector<Pane> panes;

    // Initialize I/O

    // Main loop

    // Terminate and return to terminal

    return 0;
}
